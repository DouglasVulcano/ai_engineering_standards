---
type: llm
weight: 1
---
The AGENTS.md is specific to this project: it names the Python/FastAPI stack and concrete gate
commands (for example ruff and pytest), and it does not leave template placeholders such as <cmd>
unfilled. It is concise, not a generic dump.
