# CLAUDE.md

See @AGENTS.md for the canonical, model-agnostic project guide and the AI Engineering Standards.

Keep repo-specific substance in AGENTS.md (single source of truth); this file stays thin so the
standard works for every agent tool, not just Claude Code.
